using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TextManagerScene3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        for (int i = 0; i < CameraController2.SelectedPlanet.Count; i++)
        {
            if (CameraController2.SelectedPlanet[i] == 0)
            {


            }


        }
      
    }
}
